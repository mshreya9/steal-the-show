[build]
  command = "npm run build"
  publish = "dist"

# React Router is a client-side router: every deep link (/finder, /bag, ...)
# must be served index.html so the app can route it, instead of 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
